import { MutableRefObject } from 'react';
import { Props } from '../SliderProvider/types';
import { Slide } from '../Slide/types';
export interface ISliderContext extends Props {
    sliderTrackRef: MutableRefObject<HTMLElement>;
    currentSlideIndex: number;
    setCurrentSlideIndex?: (number: any) => void;
    scrollRatio: number;
    setScrollRatio: (number: any) => void;
    goToNextSlide: () => void;
    goToPrevSlide: () => void;
    goToSlideIndex: (number: any) => void;
    slides: Slide[];
    dispatchSlide: (slide: Slide) => void;
    slideWidth?: string;
    isPaused?: boolean;
    setIsPaused: (boolean: any) => void;
}
